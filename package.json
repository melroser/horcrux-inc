{
  "name": "horcrux-inc",
  "private": true,
  "packageManager": "pnpm@8.15.0",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "build:horcrux": "uv run python tools/build_horcrux.py  --name='Robert Melrose'  --title='Senior Full-Stack Engineer'  --location='Miami, FL'  --out='apps/web/public/rob_horcrux.json'  data/sources",
    "build:mcp": "cd apps/mcp && uv pip install -r requirements.txt",
    "deploy:mcp": "cd apps/mcp && render deploy",
    "mcp": "uv run uvicorn apps.mcp.main:app --host 0.0.0.0 --port 8000"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  },
  "dependencies": {
    "react-force-graph-3d": "^1.29.0",
    "three": "^0.180.0"
  }
}
